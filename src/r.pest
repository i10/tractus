WHITESPACE = _{ " " | "\t" }
file = { SOI ~ row ~ (NEWLINE ~ row?)* ~ EOI }
row = _{ WHITESPACE* ~ line ~ WHITESPACE* }
line = { comment | empty }
comment = @{ "#" ~ character* }
empty = @{ "" }

character = @{ !NEWLINE ~ ANY }